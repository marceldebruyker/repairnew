import event from './types/event'
import post from './types/post'
import gallery from './types/gallery'
import siteSettings from './types/siteSettings'

export const schemaTypes = [event, post, gallery, siteSettings]